stages:
  - build
  - test
  - deploy

# Use an official PHP image with Composer installed
image: php:8.2-cli

services:
  - name: docker:dind
    alias: docker

variables:
  COMPOSER_CACHE_DIR: "$CI_PROJECT_DIR/.composer"

before_script:
  - apt-get update && apt-get install -y unzip git curl
  - curl -sS https://getcomposer.org/installer | php
  - mv composer.phar /usr/local/bin/composer

build_app:
  stage: build
  script:
    - echo "🏗️ Building the PHP App"
    - cd backend
    - composer install
    - cd ..
    - docker compose build
  only:
    - main
    - master

test_app:
  stage: test
  script:
    - echo "✅ No tests yet"
  only:
    - main
    - master

deploy_app:
  stage: deploy
  script:
    - echo "🚀 Add deploy script here (FTP, SSH, etc)"
  only:
    - tags

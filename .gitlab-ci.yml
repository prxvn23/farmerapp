stages:
  - build
  - test
  - deploy

# ✅ BACKEND BUILD (PHP + MongoDB)
build_app:
  stage: build
  image: php:8.2
  before_script:
    - apt-get update && apt-get install -y unzip git curl libzip-dev zlib1g-dev
    - pecl install mongodb-1.19.1
    - echo "extension=mongodb.so" > /usr/local/etc/php/conf.d/mongodb.ini
    - curl -sS https://getcomposer.org/installer | php
    - mv composer.phar /usr/local/bin/composer
  script:
    - echo "📦 Installing PHP dependencies"
    - cd backend
    - composer install --ignore-platform-req=ext-mongodb
    - cd ..
    - echo "✅ Build complete"
  only:
    - main



# ✅ FRONTEND BUILD (React + Node)
build_frontend:
  image: node:18
  stage: build
  script:
    - echo "⚛️ Building React frontend"
    - cd client
    - npm install
    - npm run build
    - echo "✅ Frontend build complete"
  only:
    - main

# ✅ TEST STAGE (optional)
test_app:
  stage: test
  script:
    - echo "✅ No tests yet"
  only:
    - main

# ✅ DEPLOY STAGE (optional)
deploy_app:
  stage: deploy
  script:
    - echo "🚀 Deployment script goes here"
  only:
    - tags
pages:
  stage: deploy
  script:
    - cd client
    - npm install
    - npm run build
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - main

stages:
  - build
  - test
  - deploy

image: node:18

# 1️⃣ Install Node + React + PHP deps
before_script:
  - apt-get update && apt-get install -y unzip git curl php php-cli php-mbstring php-xml php-curl libzip-dev
  - curl -sS https://getcomposer.org/installer | php
  - mv composer.phar /usr/local/bin/composer
  - npm install --prefix client

# 2️⃣ Build Backend (PHP)
build_backend:
  stage: build
  script:
    - echo "📦 Installing PHP dependencies"
    - cd backend
    - composer install
    - cd ..
  only:
    - main

# 3️⃣ Build Frontend (React)
build_frontend:
  stage: build
  script:
    - echo "⚛️ Building React frontend"
    - cd client
    - npm install
    - npm run build
    - cd ..
  only:
    - main

# 4️⃣ Test (optional)
test_app:
  stage: test
  script:
    - echo "✅ No tests yet"
  only:
    - main

# 5️⃣ Deploy (optional)
deploy_app:
  stage: deploy
  script:
    - echo "🚀 Deployment script goes here"
  only:
    - tags
